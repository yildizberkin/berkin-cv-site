import AdminConsole from "./admin-console";

export const metadata = { title: "Content Control — Berkin Yıldız", robots: { index: false, follow: false } };

export default function AdminPage() {
  return <AdminConsole />;
}
